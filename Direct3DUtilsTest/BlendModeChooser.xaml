<UserControl x:Class="Direct3DUtilsTest.BlendModeChooser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sp="clr-namespace:Direct3DUtils;assembly=Direct3DUtils"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="300" Width="200">

    <Grid x:Name="LayoutRoot" Background="{StaticResource DialogBrush}"  >
        <ScrollViewer>
            <StackPanel x:Name="StackPanel">
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>